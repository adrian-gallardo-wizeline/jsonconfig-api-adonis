'use strict'

/**
 * Resourceful controller for interacting with fragmentschemas
 */
class FragmentSchemaController {
  /**
   * Show a list of all fragmentschemas.
   * GET fragmentschemas
   */
  async index ({ request, response, view }) {
  }

  /**
   * Render a form to be used for creating a new fragmentschema.
   * GET fragmentschemas/create
   */
  async create ({ request, response, view }) {
  }

  /**
   * Create/save a new fragmentschema.
   * POST fragmentschemas
   */
  async store ({ request, response }) {
  }

  /**
   * Display a single fragmentschema.
   * GET fragmentschemas/:id
   */
  async show ({ params, request, response, view }) {
  }

  /**
   * Render a form to update an existing fragmentschema.
   * GET fragmentschemas/:id/edit
   */
  async edit ({ params, request, response, view }) {
  }

  /**
   * Update fragmentschema details.
   * PUT or PATCH fragmentschemas/:id
   */
  async update ({ params, request, response }) {
  }

  /**
   * Delete a fragmentschema with id.
   * DELETE fragmentschemas/:id
   */
  async destroy ({ params, request, response }) {
  }
}

module.exports = FragmentSchemaController
